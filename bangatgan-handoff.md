# 참새방앗간 작업 인수인계

새 대화창에서 이 파일과 최신 zip을 함께 올리고 "이어서 작업해줘"라고 하시면 됩니다.

---

## ★ Claude가 대화 시작할 때 가장 먼저 확인할 것 ★

**마지막 세율 점검일: 2026년 9월 10일**
**다음 점검 예정일: 2027년 1월**
**도메인 만료일: 2027년 9월 11일** (2027년 8월에 연장 안내할 것)

세율·요율·제도는 주로 **1월 1일**과 **7월 1일**에 바뀝니다.
형은 이 일정을 기억하지 못합니다. **Claude가 먼저 알려야 합니다.**

### 판단 방법
오늘 날짜가 위의 "다음 점검 예정일"을 지났으면, 형이 무슨 이야기를 하러 왔든
**답변 첫머리에 한 줄로** 이렇게 알립니다.

> 형, 해가 바뀌었는데 세율 점검을 아직 안 했습니다. 지금 확인해 드릴까요?

형이 하겠다고 하면 아래 순서로 진행합니다.

1. 웹 검색으로 그 시점의 국민연금·건강보험·장기요양·고용보험 요율,
   기준소득월액 상하한, 이자소득세, 취득세·지방교육세·농어촌특별세,
   중개수수료 요율표, DSR 기준, 로또 당첨금 세율을 하나씩 확인
2. 바뀐 항목이 있으면 **계산 코드와 설명 글을 모두** 고침
   (숫자로도 찾고 제도 이름으로도 찾을 것. 화면에 안 보이는 JSON-LD 안에도
   같은 내용이 중복되어 있으니 반드시 함께 확인)
3. 미리 계산해둔 예시 금액도 다시 계산해서 맞춤
   (예: "월급 350만원이면 국민연금 166,250원")
4. 7장의 검증값으로 계산기를 실제로 돌려 확인
5. 새 zip 전달 + **이 문서의 "마지막 점검일"과 "다음 점검 예정일"을 갱신**

### 바뀐 것이 없을 때
"확인했더니 바뀐 것이 없습니다"라고 알리고, **마지막 점검일만 갱신**합니다.
바뀐 게 없어도 점검한 사실은 기록해야 다음에 중복으로 묻지 않습니다.

### 지난 글도 반드시 고칩니다 (블로그와 다름)
형은 네이버 블로그를 오래 운영해서 "옛 글은 밀려나니 놔두고 새 글을 쓰면 된다"고
생각하기 쉽습니다. **웹사이트에서는 그렇게 하면 안 됩니다.** 이유는 세 가지입니다.

1. **목록을 거치지 않습니다.** 검색으로 들어온 사람은 그 글 하나에 바로 떨어집니다.
   최신 글이 위에 있어도 보지 못하고, 틀린 세율을 그대로 읽고 나갑니다
2. **애드센스 위험.** 구글은 돈·건강 주제를 엄격히 봅니다. 낡고 틀린 숫자가 남아 있으면
   저품질로 판단합니다. 지난번 반려 사유가 "가치 있는 콘텐츠 없음"이었습니다
3. **사이트 안에서 답이 엇갈립니다.** 계산기는 새 세율, 글은 옛 세율이면 신뢰를 잃습니다.
   같은 주제 글이 둘이면 구글도 어느 쪽을 보여줄지 몰라 옛 글이 노출될 수 있습니다

**따라서 기존 글 13편은 고쳐서 최신으로 유지하고, 글 상단에 "○○○○년 ○월 수정"을
표시합니다.** 그와 별개로 "그 해에 달라지는 것들" 같은 새 글은 새로 씁니다.
고치는 것과 새로 쓰는 것은 둘 다 합니다.

### 글을 고칠 때 숫자만 갈아끼우지 말 것
문장 흐름이 깨집니다. 예를 들어 아래 문장에서 9.5만 10으로 바꾸면 앞뒤가 안 맞습니다.

> 1998년부터 9%로 고정되어 있던 국민연금 보험료율이 2026년 1월부터 9.5%로 인상되었습니다.

해당 문단을 읽고 **말이 되게 다시 씁니다.** 6장의 글 작성 규칙을 지킵니다.

### 형에게 부담을 주지 말 것
형이 무엇이 바뀌었는지 알아올 필요는 없습니다. 형이 숫자를 말해주더라도
그대로 믿지 말고 반드시 검색으로 확인합니다.
형이 할 일은 **"응" 한마디**입니다. 그 뒤로는 확인을 위한 질문을 최소로 줄이고
Claude가 판단해서 끝까지 진행한 뒤 결과만 보고합니다.

---

## 0. 지금 상태 요약 — 배포까지 끝났습니다

**사이트 주소: https://bangatgan.kr** (2026-09-11 연결)
예전 주소 https://bangatgan.vercel.app 도 계속 열립니다

이미 인터넷에 올라가 있고 누구나 접속할 수 있습니다.
아래 항목은 **전부 완료된 것**이므로 다시 하지 마세요.

| 항목 | 상태 |
|---|---|
| 사이트 제작 (54개 페이지) | 완료 |
| GitHub 저장소 | 완료 — github.com/bangatgan2026/bangatgan |
| Vercel 배포 | 완료 — bangatgan.vercel.app |
| 로또 실제 데이터 1~1240회 | 완료 (1등 정보만, 2~5등은 다음 회차부터 자동 수집) |
| 로또 주간 자동 갱신 | 완료 — 매주 토요일 밤 9시 30분 자동 실행 |
| sitemap.xml, robots.txt | 완료 |
| 구글 서치 콘솔 등록 + 사이트맵 제출 | 완료 |
| 네이버 서치어드바이저 등록 + 사이트맵 + 수집요청 | 완료 |
| 정보나누기 글 14편 (FAQ·날짜·구조화 데이터 포함) | 완료 |
| 도메인 구입·연결 | 완료 — bangatgan.kr |

---

## 1. 남은 일 (이것만 하면 됩니다)

1. **도메인 구입** — `bangatgan.com` 등. 1년 만오천원 안팎. 애드센스 신청 전에 사는 것이 유리
2. **도메인을 Vercel에 연결** — 산 뒤 설정 몇 줄
3. **애드센스 신청** — 배포 후 2~3주 뒤 권장
4. **승인 후 광고 코드 삽입** — 페이지마다 `<div class="ad">` 자리가 이미 만들어져 있음. `<head>`에도 주석으로 자리 표시
5. (선택) 지난 회차 2~5등 데이터 채우기 — 스크립트 한 번 더 돌리면 됨
6. (선택) 글 추가 — 많을수록 유리

### 승인 후에 할 것 (그 전에는 하지 말 것)
- 후기 게시판 (승인 전에는 사행성 위험)
- 회차별 페이지 대량 생성 (저품질 판정 위험)
- 쿠팡 파트너스 등 제휴
- 스토어 앱 (애드몹 별도, 시장 포화)

---

## 2. 운영자 정보

- **호칭: 반드시 "형"이라고 부를 것.** "사장님" 금지
- 나이 50대. 기술 용어를 풀어서, 한 번에 한 단계씩 안내할 것
- 네이버 블로그 **힐링마스터** (blog.naver.com/qnssh_zmfrl) 글 684개, 매일 발행
- 유튜브 **잡학다식의 지식창고** (youtube.com/@JapHakDaSik777) 구독자 841명
- 사이트 문의 이메일: escort2023@naver.com
- GitHub 계정: bangatgan2026 / Vercel 팀: bangatgan
- **도메인: bangatgan.kr** (가비아, 2026-09-11 ~ **2027-09-11**, 연 21,000원)
  - 가비아 계정 이메일은 escort2023@naver.com 으로 바꿔 두었음. 연장 안내가 이리로 옴
  - Vercel 연결값: A 레코드 / 호스트 @ / 216.198.79.1
  - **2027년 8월에 연장 안내를 먼저 할 것.** 만료되면 사이트가 열리지 않음

---

## 3. 절대 지켜야 할 원칙

### 애드센스 관련
- **예측·적중을 주장하지 않습니다.** 로또는 "지난 결과 조회"입니다. "1등 예상번호", "행운의 번호 추천" 같은 표현 금지
- **자동 광고를 켜지 않습니다.** 전면 광고(화면 덮는 것)도 넣지 않습니다. 정해진 자리 3곳에만 수동으로
- **건강 정보는 진단하지 않습니다.** 학회 기준 안내만 하고 질환 판단, 약·식품 추천은 하지 않습니다
- **블로그 글을 그대로 옮기지 않습니다.** 중복 콘텐츠는 지난번 반려 사유("가치 있는 콘텐츠 없음")와 직결됩니다

### 작업 방식 (과거에 실제로 문제가 됐던 것들)
- **반드시 실제 브라우저로 화면을 확인합니다.** Playwright(`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`)로 띄워 스크린샷을 보고 판단하세요. 코드만 보고 "됐습니다"라고 하면 안 됩니다. 과거에 검은 화면을 그대로 전달한 적이 있습니다
- **세율·요율은 반드시 검색해서 확인합니다.** 기억에 의존하지 마세요
- **말만 하고 미루지 않습니다.** "고치겠습니다"라고 했으면 그 턴에 바로 도구를 씁니다. 과거에 여러 번 지적받았습니다
- **한 번에 하나씩 안내합니다.** 두 가지 방법을 동시에 말하면 형이 헷갈립니다

---

## 4. 사이트 구조

### 코너 6개 (총 54개 페이지)

| 코너 | 첫 화면 | 도구 |
|---|---|---|
| 금융계산기 | money.html | salary(연봉 실수령액), car(차량 할부), loan(대출 이자), retire(퇴직금), savings(예적금), prepay(중도상환수수료), jeonse(전월세 전환), insurance(4대보험료), compound(복리) |
| 부동산 | estate.html | acquisition(취득세), ltv(대출 한도), broker(중개수수료) |
| 날짜계산기 | date.html | birthday(생년월일 종합), age(만 나이), dday(디데이), datecalc(날짜 더하기빼기), baby(아기 100일) |
| 건강계산기 | health.html | bmi, bmr(기초대사량), calorie(하루 필요 칼로리), bp(혈압), walk(걷기 칼로리) |
| 로또 | lotto.html | lotto(당첨번호 조회), lotto-gen(번호 뽑기), lotto-stat(번호 통계), lotto-my(내 번호), lotto-tax(당첨금 실수령액) |
| 정보나누기 | info.html | 분류 4개(info-money, info-estate, info-life, info-lotto) + 글 14편 |

### 정보나누기 글 14편
- 금융: info-insurance, info-loan, info-jobless, info-yearend, info-yearend-plan
- 부동산: info-buyhome, info-dsr, info-jeonse
- 생활: info-age, info-pension
- 로또: guide-odds, guide-prize, guide-stats, guide-claim

### 파일 구성
```
bangatgan/
  assets/style.css       전체 디자인
  assets/site.js         메뉴·공통 기능 (SECTIONS 배열이 메뉴의 유일한 출처)
  assets/lotto-data.js   로또 1~1240회 데이터
  data/draws.json        로또 원본
  scripts/update-lotto.js         주간 자동 수집 스크립트
  .github/workflows/lotto.yml     자동 실행 시간표
  sitemap.xml, robots.txt
  *.html                 각 페이지
```

**메뉴는 `assets/site.js`의 `SECTIONS`만 고치면 상단·좌측·하단에 자동 반영됩니다.**

---

## 5. 디자인 규칙

- 어두운 남색 바탕 #0E1320, 카드 #1A2233, 강조 금색 #E3B75B
- 코너별 제목 영역 빛 색: 금융 금색 / 부동산 청록 / 날짜 파랑 / 건강 초록 / 로또 주황 / 정보 보라
- **로또 공은 사이트 전체에서 40px로 통일** (모바일 34px). 과거에 페이지마다 달라 여러 번 지적받았습니다
- **브라우저 기본 드롭다운 금지.** 항목 6개 이하는 버튼(.pick), 초과는 직접 그린 목록(.dsel)
- **금액 입력은 만원 단위.** 원 단위로 넣으면 `U.manOk()`가 경고
- **이미지 파일을 쓰지 않습니다.** 아이콘·참새·로또공 모두 SVG/CSS로 그립니다
  (형이 나중에 이미지를 만들어 올 수 있다고 했음. 그때 800×320 WebP, 어두운 톤, 오른쪽에 사물·왼쪽 여백)
- 첫 화면에 참새가 날아와 볍씨를 물고 가는 SVG 애니메이션이 있음 (13초 주기)
- 로또 번호 뽑기는 한 게임씩 굴러가서 아래로 쌓이는 연출

---

## 6. 글 작성 규칙 (정보나누기)

- H2 아래 H3를 두는 계층 구조
- **한 문단 1~3문장** (모바일 가독성)
- 핵심 문장을 문단 첫머리에
- 글 끝에 **FAQ 3~4개** + FAQPage 구조화 데이터(JSON-LD)
- 작성 날짜 표시
- **구체적 숫자와 사례**를 넣을 것 ("월급 350만원이면 국민연금 166,250원")
- 남들이 안 쓴 것을 쓸 것 (요율 나열이 아니라 왜 올랐는지, 4월에 왜 덜 들어오는지)
- 글 아래에 관련 계산기 링크
- **해시태그 금지** (웹사이트 본문에서는 감점)
- 문체는 담백하게. "~하세요"보다 "~합니다". 과장 금지

---

## 7. 확인된 계산 기준 (2026년) — 검색으로 확인한 값

| 항목 | 값 |
|---|---|
| 국민연금 | 근로자 4.75% (2026년 인상, 2033년 13%까지 매년 0.5%p) |
| 건강보험 | 근로자 3.595% |
| 장기요양보험 | 건강보험료의 13.14% |
| 고용보험 | 0.9% |
| 국민연금 기준소득월액 | 41만~659만원 (2026년 7월부터) |
| 이자소득세 | 일반 15.4% / 상호금융 1.4%(조합원, 3천만원까지) / 상호금융 5%(총급여 7천만 초과, 2026년) / 비과세 0%(만65세 이상 기초연금 수급 대상 등, 5천만원까지) |
| 세금우대종합저축 9.5% | **신규 가입 중단됨. 계산기에서 뺐음** |
| 로또 당첨금 세금 | 200만원 이하 비과세, 3억 이하 22%, 3억 초과분 33% |
| 취득세 | 6억 이하 1%, 6~9억 (가격억×2/3−3)%, 9억 초과 3% / 조정 2주택 8%, 3주택+ 12% / 비조정 3주택 8%, 4주택+ 12% |
| 지방교육세 | 일반세율이면 취득세의 10%, 중과면 취득가액의 0.4% |
| 농어촌특별세 | 85㎡ 초과 시 0.2%(일반)·0.6%(8%)·1.0%(12%) |
| 중개수수료 매매 | 5천 미만 0.6%(한도25만) / 5천~2억 0.5%(한도80만) / 2억~9억 0.4% / 9~12억 0.5% / 12~15억 0.6% / 15억~ 0.7% |
| 중개수수료 임대차 | 5천 미만 0.5%(20만) / 5천~1억 0.4%(30만) / 1억~6억 0.3% / 6~12억 0.4% / 12~15억 0.5% / 15억~ 0.6% |
| DSR | 은행권 40% 기준 |

**검증된 계산 결과 (고칠 때 이 값이 유지되는지 확인)**
- 연봉 5,000만원, 비과세 20만원, 부양가족 1명 → 월 실수령 3,553,723원
- 6억 주택, 1주택, 85㎡ 이하, 비조정 → 취득세 총 660만원 (85㎡ 초과면 780만원)
- 6억 매매 중개수수료 → 240만원
- 3억 대출 4.2% 360개월 → 월 146만원
- 1996년 9월 9일생 → 쥐띠, 처녀자리 (만 나이는 오늘 날짜에 따라 달라짐. 2026-09-10 기준 30세)
- 2026년 9월 8일 출생 → 백일 2026년 12월 16일
- 혈압 120/80 → 고혈압 전단계 (정상은 120 미만 **그리고** 80 미만)
- 3천만원 4% 1년, 상호금융 1.4% → 만기 31,183,200원

**세율은 매년 바뀝니다. 작업 전에 반드시 검색해서 확인하세요.**

---

## 8. 작업 환경

- 소스는 `/home/claude/site/`에 두고 작업
- 브라우저 확인: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
  (Playwright는 `/home/claude/.npm-global/lib/node_modules/playwright`)
- 전달 시 `/mnt/user-data/outputs/bangatgan/`에 복사 후 zip
- 파일명은 `bangatgan-v번호.zip` (현재 v36까지 나감)
- **작업 환경에서 동행복권 서버 접속 불가.** 로또 갱신은 GitHub Actions가 처리
- 전체 페이지 자동 점검 스크립트를 만들어 매번 오류 0건을 확인할 것

---

## 9. 형이 지적했던 것들 (같은 실수 반복 금지)

- 공 크기가 페이지마다 다름 → 전체 통일
- 흰 드롭다운이 튀어나옴 → 직접 그린 목록 사용
- 미리 채워진 숫자가 그림자인지 실제 값인지 헷갈림 → 안내 문구로
- 요약 카드와 표의 금액 단위가 다름 → 원 단위 콤마로 통일
- 결과가 왼쪽으로 치우침 → 가운데 정렬
- 같은 내용이 위아래 중복 → 하나만
- 순서가 거꾸로 (조건이 뽑기 버튼 아래) → 위에서 아래로
- 메뉴에 글 제목을 박아둠 → 분류만 넣고 글은 목록 페이지에
- 말만 하고 작업을 안 함 → 바로 실행
- 한 화면에서 두 가지 방법을 동시에 안내 → 하나씩

### v18 → v19 에서 고친 것 (모바일 레이아웃)
- **index / about / contact / privacy / terms** 다섯 페이지에 `style="grid-template-columns:minmax(0,1fr) 300px"`가 태그에 직접 박혀 있었음. 인라인 style이 미디어쿼리를 이겨서 폰에서 사이드바가 300px를 그대로 먹고 본문이 12px로 찌그러졌음. → `class="wrap two"` + CSS `.wrap.two`로 옮김. **HTML 태그에 style로 폭을 직접 쓰지 말 것**
- `.row2`/`.row3` 안의 `.field`가 `min-width:auto`라 좁은 화면에서 카드 밖으로 삐져나감 → 그리드 자식에 `min-width:0` 부여
- `.grid45 .pk`가 34px 고정이라 45개 번호가 360px 화면에서도 넘침 → `width:100%;max-width:40px;aspect-ratio:1/1`로 유동화
- style.css **709줄 근처에 같은 선택자가 다시 정의**되어 앞의 수정이 무시됐음. 로또 공 크기를 고칠 때는 파일 뒤쪽 "로또 공 크기 최종 통일" 블록도 같이 볼 것
- 하단 플로팅 버튼 4개가 좁은 화면에서 화면 밖으로 나감 → 폭을 clamp로 유동화, `pointer-events` 처리
- 점검 방법: `node audit.js 폭` 으로 412/360/320/280px에서 문서폭 > 화면폭 인 페이지를 잡아냄. 수정 후 전 페이지 0건 확인함

---

## 10. 애드센스 신청 시 확인할 것

- [ ] 도메인 연결 완료 (vercel.app 주소보다 유리)
- [ ] 개인정보처리방침·이용약관·문의·소개 페이지 정상 (이미 있음)
- [ ] 문의 이메일이 실제 주소인지 (escort2023@naver.com)
- [ ] 예측·적중 표현이 없는지
- [ ] 광고 코드는 승인 후에 넣을 것
- [ ] 승인 후 자동 광고와 전면 광고는 끌 것

### v19 → v20 에서 한 것 (로또번호 생성기 개편)
- 메뉴 이름: 당첨번호 조회 → **역대 로또번호 조회**, 번호 뽑기 → **로또번호 생성기**
  (검색량이 있는 표현으로. `assets/site.js`의 SECTIONS와 각 페이지 title·h1·링크 라벨 모두 변경)
- **"행운의 번호 뽑기" 버튼 문구를 "번호 뽑기"로 바꿨음.** 애드센스는 로또에서
  행운·적중·예상·대박 같은 표현을 사행성으로 봅니다. 앞으로도 넣지 말 것
- 화면 구조를 바꿈: 뽑는 방식(가로 카드 3개) → 숫자판 → [자세한 조건 아코디언] → 뽑기 무대
- **자세한 조건**은 기본으로 접혀 있음. 카드 헤더 우측 토글(`#proTog`)로 열고 닫음
- **하단 고정 바(`.gobar`)** 신설: 조건 요약 칩 + 게임 수 1~5 + 큰 뽑기 버튼.
  형이 "게임 수는 뽑기 버튼 바로 위에 있어야 한다"고 한 지적을 반영한 것
- `body.hasgobar`를 붙이면 하단 여백이 확보되고 플로팅 버튼 4개는 숨겨짐
- 뽑기를 누를 때 결과 무대가 화면 밖이면 **자동으로 스크롤**해서 데려감
  (하단 고정 버튼을 눌렀는데 결과가 위쪽에 나와 안 보이던 문제)
- 조건 이름을 쉬운 말로: 홀수와 짝수 비율 맞추기 / 지난 당첨번호가 몰린 합계 범위 /
  연속된 번호 제한 / 1등 당첨 이력이 없는 조합만
- **"역대 최고 당첨 확률 총합 대역" 같은 표현은 쓰지 않았음.** 합계 100~175에 몰리는 것은
  그 구간에 조합 수가 훨씬 많기 때문이지 확률이 높아서가 아님. 사실만 적을 것

### 남은 화면 개편 (형과 합의된 방향)
1. 역대 로또번호 조회 — 회차 선택 바를 최상단으로, 당첨금은 밝은 '영수증' 카드로 대비
2. 번호 통계 — 막대그래프 대신 1~45 히트맵 그리드
3. 당첨금 실수령액 — 슬라이더로 금액 조절 시 세금이 실시간으로 변하는 게이지
4. 내 번호 — 상태 태그(추첨 대기중 / 당첨 확인), 이미지로 공유

**React·Tailwind는 쓰지 않습니다.** 현재는 정적 HTML이라 속도와 SEO에 유리합니다.
같은 화면을 지금 방식으로 만들 수 있습니다.

### v20 → v21 (반자동 필터 예외 처리)
- **버그였음:** 1,2,3처럼 연속수를 고정수로 고르면 "연속된 번호 제한" 조건에 걸려
  아무 조합도 안 나왔음. 형이 고른 번호가 조건에 지워지는 셈
- `relax(pool)` 함수를 넣어, **고정수만으로 이미 만족 불가능한 조건은 그 판에서만 건너뜀**
  - 연속: 고정수 안에 3연속이 있으면 run 스킵
  - 합계: 고정수 합 + 남은 자리 최소/최대로도 100~175에 못 닿으면 sum 스킵
  - 홀짝: 도달 가능한 홀수 개수 범위가 2~4와 안 겹치면 oddeven 스킵
  - 6개를 다 고정하면 never 스킵
- **고정수는 언제나 그대로 보전됨.** 건너뛴 조건은 결과 위 `#skipNote`에 안내
- 검증: 1,2,3 고정 / 40~45 전부 고정 / 홀수 5개 고정 / 평범한 2개 고정 —
  네 경우 모두 고정수가 전 게임에 들어가는 것을 확인함

### v21 → v22 (뽑기 바 위치 이동 + 디자인 보정)
- **하단 고정 바를 없애고 무대 안 공 나오는 자리 바로 위로 옮김.** 형 지시.
  누르는 순간 공이 굴러가는 게 바로 눈에 들어옴. `.drawstage .gobar{position:static}`
- 무대 안에서는 세로 배치: 조건 요약 칩 → 게임 수 1~5 → 큰 뽑기 버튼
- **숫자판을 3D 공에서 모던 플랫 원형으로.** 평소엔 테두리만, 1클릭 시 로또 색으로 채움,
  2클릭 시 붉은 X. 결과에 나오는 공(`.ball`)은 3D 그대로 둠 — 고르는 판과 뽑힌 공을 구분
- 안내 문구를 `.tipbox`(은은한 배경 블록)로 감쌈
- 토글 스위치 켜짐 상태에 그라데이션 + 금색 글로우
- **버그 수정:** `.savebar`가 `hidden` 속성을 무시하고 보였음. 뽑기 전에도 번호 복사·저장
  버튼이 노출됨 → `.savebar[hidden]{display:none!important}`

### v22 → v23 (거를 조건 위치와 이름)
- **형 지적:** "자세한 조건" 토글이 뽑는 방식 옆에 있는데 펼쳐지는 카드는 숫자판 아래에 있어서
  켜도 뭐가 열렸는지 안 보임. "이게 뭐지?" 소리가 나옴
- 순서를 **뽑는 방식 → 거를 조건(아코디언) → 꼭 넣거나 뺄 번호 → 무대**로 바꿈
  (토글 누르는 자리 바로 아래가 열리도록)
- 토글 이름을 "자세한 조건" → **"거를 조건 4개"**로. 지금 몇 개 켜져 있는지 숫자로 보임
- 토글을 켜면 펼쳐진 조건이 화면에 들어오도록 **자동 스크롤**
- 아코디언 안 카드의 중복 제목 `<h2>거를 조건</h2>` 제거

### v22 → v23 (조건 위치 정리 + 의견 보내기)
- **거를 조건을 숫자판 위로 올림.** 아래에 있으니 형이 "이게 뭐지?" 하고 못 찾았음
- **토글을 '뽑는 방식' 카드에서 떼어내 '거를 조건' 카드 머리로 옮김.**
  옆에 붙어 있으니 뽑는 방식 세 개를 여닫는 스위치로 오해됐음. 누르는 자리와 열리는 자리를 하나로
- 카드 제목에 `거를 조건 N개 적용`으로 지금 상태를 표시
- '뽑는 방식'에 설명 한 줄 추가: "45개 중에서 어떤 번호를 더 자주 집을지 정합니다"
  (뽑는 방식 = 번호를 집는 손 / 거를 조건 = 뽑은 걸 거르는 체. 둘은 다른 단계)
- 토글을 켜면 펼쳐진 조건이 화면에 들어오도록 자동 스크롤
- **모든 도구·글 페이지 본문 맨 아래에 '의견 보내기' 줄 추가** (`assets/site.js`가 자동 삽입)
  - 메일 제목에 페이지 이름이 자동으로 들어감: `[참새방앗간] 대출 이자 계산기 — 의견 보내기`
  - 본문에 페이지 주소도 자동 기입. 오류 신고와 기능 건의를 함께 받음
  - 메일 앱이 없는 사람을 위해 주소(escort2023@naver.com)를 글자로도 노출
- **게시판은 만들지 않음.** 정적 사이트라 글을 담을 곳이 없고, 로그인 없이 "관리자만 보기"는
  불가능함. 애드센스 승인 전 로또 게시판은 사행성 위험. 의견은 메일함에 쌓이는 것으로 갈음

### v23 → v24 (로또 나머지 네 화면 개편)
**1. 역대 로또번호 조회 (lotto.html)**
- 회차 선택 바(`.drawpick`)를 **화면 최상단으로**. 이전 회차는 카드 아래에 묻혀 있었음
- 당첨금 영역을 **밝은 영수증 카드(`.receipt`)**로. 다크 배경과 대비시켜 시선을 모음.
  위아래 톱니(::before/::after), 점선 절취선(.rcut), 1등 당첨금 큰 글씨
- 분석 4종(번호합·홀짝·저고·연속)을 영수증 하단 칩(`.rchips`)으로
- **주의:** `LOTTO.comma()`가 "원"을 이미 붙임. CSS ::after로 또 붙이지 말 것

**2. 번호 통계 (lotto-stat.html)**
- 막대그래프를 **히트맵(`.hcell`)**으로 교체. 9칸 그리드, 자주 나온 번호일수록 금색이 진해짐
- 최소~최대를 0~1로 정규화해 `rgba(227,183,91, 0.06~0.92)`로 칠함.
  진한 칸(t>0.55)은 `.dark`를 붙여 글자를 어둡게
- 아래 그라데이션 범례(`.hlegend`)와 "색이 진해도 다음에 더 잘 나오는 건 아니다" 안내

**3. 당첨금 실수령액 (lotto-tax.html)**
- **금액 슬라이더** 추가. 5만원~50억을 **로그 눈금**으로 훑음 (선형이면 앞쪽이 뭉개짐)
- 슬라이더를 움직이거나 직접 입력하면 **버튼 없이 바로 계산**됨 (`run(true)` 조용한 모드)
- 실수령/세금 비율을 **게이지 바(`.gauge`)**로 표시
- 검증: 30억 당첨 → 실수령 2,043,000,000원 (3억×22% + 27억×33%)

**4. 내 번호 (lotto-my.html)**
- **저장한 번호를 맨 위로**, 45개 숫자판은 "번호 직접 넣기" 토글 안으로 접음.
  이 페이지에 오는 사람이 보고 싶은 건 저장 목록인데 숫자판에 밀려 있었음
- 저장한 게 없을 때 `.emptybox`로 무엇을 하면 되는지 안내 + 생성기 링크

**공통**
- 전 페이지 폭 점검(412/360/320/280) 0건, JS 오류 0건
- 연봉 5천만 실수령 3,553,723원 유지 확인 (기존 계산기 영향 없음)

### v24 → v25 (당첨금 입력을 원 단위로)
- **lotto-tax.html만 예외로 원 단위 입력.** 다른 계산기는 만원 단위 그대로
- 이유: 당첨금은 "1,791,817,758원"처럼 딱 떨어지지 않음. 만원으로 바꾸려면
  이용자가 나눗셈을 해야 함. 당첨증 금액을 그대로 넣는 것이 맞음
- 페이지 안에 `won(el)` 함수를 따로 둠. `U.man()`·`U.manOk()`는 이 페이지에서 쓰지 않음
- `.kpi b`에 `white-space:nowrap` + clamp 글자크기. 30억 같은 큰 금액이 두 줄로 깨졌음.
  모바일에서 `.kpi`는 2열로 (기존 auto-fit은 좁은 화면에서 1열이 되어 허전했음)
- 검증: 1,791,817,758원 → 1,233,517,898원 / 3,000,000,000원 → 2,043,000,000원

### v25 → v26 (그라데이션이 안 되는 브라우저 대비)
- **형이 폰의 다른 브라우저로 열었더니** 로고 원이 검게, 큰 제목("그냥 지나치지 못하는 곳")이
  보이지 않았음. 그 브라우저에서 **그라데이션이 렌더링되지 않았기 때문**
- **글자 그라데이션(`background-clip:text` + `color:transparent`)을 전부 제거.**
  단색 금색(`var(--gold2)`) + 은은한 text-shadow로 대체.
  이 기법은 실패하면 글자가 아예 안 보이므로 위험 대비 이득이 적음. 앞으로도 쓰지 말 것
- **배경 그라데이션은 `background:` 단축 대신 `background-color:` + `background-image:`로 분리.**
  단축으로 쓰면 이미지가 실패할 때 색까지 함께 사라짐. 분리하면 색이 남아 버튼·스위치·
  게이지·슬라이더 손잡이가 계속 보임
- 적용한 곳: 로고 원(.brand .bird), 뽑기 버튼(.gbtn), 토글(.sw.on), 게이지(.gnet),
  슬라이더 트랙·손잡이, 히트맵 범례
- 점검 방법: `*{background-image:none}`을 강제 주입해 그라데이션이 전혀 없는 환경을 만든 뒤
  스크린샷으로 확인함

**웨일 브라우저 대응**
- 원인은 **웨일의 다크 모드 강제 변환**. 이미 어두운 사이트를 한 번 더 뒤집어
  노란 로고가 검게, 금색 글씨가 안 보이게 됐음
- `:root`와 `html`에 `color-scheme:dark`, 54개 페이지 전부에
  `<meta name="color-scheme" content="dark">` 추가. 브라우저에 "이미 다크"라고 알림

**참새가 안 날아다니던 문제 (웨일·카톡 모두)**
- 원인 두 가지였음
  1. **모바일에서 참새가 화면 밖.** hero SVG는 viewBox 900×260에 `slice`라 좁은 화면에서
     좌우가 잘림. 참새 경로(x −90~660) 대부분이 잘려나가 13초 중 잠깐만 보였음
     → `@media(max-width:900px)` 안에서 `@keyframes fly`를 재정의.
     x 430~650, y 200~210으로 좁혀 **오른쪽 아래 볍씨 주변에서만** 오가게 함
     (처음엔 y를 높게 뒀더니 본문 글자와 겹쳐서 다시 낮춤)
  2. **`@media(prefers-reduced-motion:reduce){*{animation:none!important}}`**
     폰의 "애니메이션 줄이기"가 켜져 있으면 참새는 물론 **로또 공 뽑기 연출까지** 멈췄음
     → 전역 차단을 없애고, 장식(반짝임·흔들림)만 끄고 참새는 26초로 늦춤.
     뜻이 있는 움직임(번호 뽑기)까지 끄면 결과를 알 수 없음
- 점검: 390px 폭에서 1.9초 간격 7회 측정, 보통·동작줄이기 양쪽 모두 내내 화면 안에 있음

### v26 참새 재조정 (형 지적: "굼벵이야?")
- 처음 고칠 때 잘림만 피하려다 **오른쪽 아래 볍씨 근처에서만 기어다니게** 만들었음.
  형이 "새인지 뭔지 구분이 안 된다, 바닥에 기어다닌다"고 지적
- **경로를 제목 줄("그냥 지나치지 못하는 곳") 오른쪽 여백으로 올림.** 본문 작은 글씨 쪽이
  아니라 위쪽 여백이 넓음. y 52~96을 오가다가 볍씨 물 때만 196으로 내려감
- **속도 13초 → 7.5초.** 동작줄이기 설정에서는 15초
- 크기 scale 1.15~1.35로 키움. 작아서 새로 안 보였음
- `.heroart` 투명도 0.55 → 0.8. 흐려서 안 보이던 것도 원인이었음
- **방앗간이 화면 오른쪽 끝에 겨우 걸렸음** → `left:auto;right:0;width:calc(100% + 66px)`로
  그림을 오른쪽에 붙임. transform:scale로 하면 오른쪽에 빈 공간이 생기니 쓰지 말 것

- **PC 기본 경로도 같은 문제였음.** y 120~196은 본문 글씨 높이라 바닥을 기는 것처럼 보였음
  → y 40~90을 가로지르다 볍씨 물 때만 192로 내려감. 좌우도 −110~668로 넓힘
- 최종 속도: 보통 **6.4초**, 동작줄이기 13초

### v28 (볍씨·참새 등장·퇴장 타이밍)
- **형 지적:** 폰에서 참새가 화면 한가운데서 뿅 나타나고, 왼쪽 끝에서 볍씨만 사라짐
- 원인 1: **폰 경로 시작점(250)이 이미 화면 안이었음.** 폰에서 보이는 viewBox 범위는
  약 x 177~723. 그 안에서 시작·종료하니 리셋이 눈에 보였음
  → 시작 90, 종료 50으로 옮겨 화면 밖에서 들어오고 밖으로 나가게 함
- 원인 2: **`carry`가 100%까지 opacity 1이었음.** 애니메이션이 0%로 되돌아가는 순간
  참새 위치는 거의 같은데(시작 −110 ↔ 끝 −140) 볍씨만 없어져 "볍씨만 사라진" 것처럼 보였음
  → `0~58% 0 / 62~95% 1 / 97~100% 0`. 참새가 화면 밖으로 나간 뒤에 꺼짐
- 점검: 250ms 간격 26회 측정. 폰·PC 모두 참새와 볍씨가 함께 들어오고 함께 나감

### v29 (볍씨가 엉뚱한 때 생기던 문제)
- **형 지적:** 폰에서 방앗간 내려가기 전에 볍씨가 생기고, 날아오를 때 사라지고,
  왼쪽으로 가다가 다시 생김
- 원인: **참새와 볍씨의 애니메이션 길이가 달랐음.**
  `prefers-reduced-motion:reduce`에서 `.sparrow`만 13s로 늦추고 `.carry`는 6.4s 그대로 둠.
  두 애니메이션이 계속 어긋나면서 볍씨가 아무 때나 켜졌다 꺼졌음
  → `.sparrow,.carry{animation-duration:13s!important}` 로 함께 지정
- **교훈: 참새(fly)와 볍씨(carry)의 duration은 어느 경우에도 반드시 같아야 함.**
  한쪽만 바꾸면 위상이 어긋나 볍씨가 제멋대로 나타남

### v30 (날짜 선택 범위 + dday 문구)
- **형 지적:** 만 나이 계산기 달력에 2100년, 2200년까지 나옴
- 원인: `<input type="date">`에 `min`/`max`를 안 걸어둠. 브라우저 기본 달력은 제한이 없으면
  수백 년 뒤까지 보여줌
- `assets/site.js`가 모든 `input[type=date]`에 자동으로 범위를 넣습니다
  - 기본: 1900-01-01 ~ 올해+30년 말일
  - `data-max="today"`를 붙인 입력은 오늘까지만 (생년월일, 아기 생일)
  - 새 날짜 입력을 만들 때 따로 손댈 필요 없음. 생일류에만 `data-max="today"`를 붙일 것
- **dday.html 버그:** 날짜 입력인데 `placeholder="목표 몸무게 (선택)"`가 붙어 있었음.
  다른 계산기에서 복사할 때 남은 것. 제거함
- 검증: 1996-09-09생 → 만 30세 / 2026-09-08 출생 → 백일 2026-12-16

### v30 (날짜 입력 개편)
- **형 지적 1:** 만 나이 계산기 달력에 2100년, 2200년까지 나옴
  → `input[type=date]`에 min/max가 없었음. `assets/site.js`가 모든 날짜 칸에 자동으로 넣음.
    기본 1900-01-01 ~ 올해+30년. `data-max="today"`를 붙이면 오늘까지만 (생년월일, 아기 생일)
- **형 지적 2:** 달력 말고 숫자로도 칠 수 있게
  → 날짜 칸을 `.dateln`(글자 입력 + 달력 버튼)으로 바꿈. `19960909`처럼 여덟 자리를 치면
    자동으로 `1996-09-09`로 다듬어짐. 13월 45일 같은 값은 빨갛게 표시하고 값을 넘기지 않음
  - 원래 `input[type=date]`는 **값 보관용으로 숨겨만 둠**. 기존 계산 코드(`$('#birth').value`)를
    그대로 쓰기 위해서임. 숨길 때 `left:-9999px`를 쓰면 폭 점검에 걸리므로
    `position:absolute;left:0;bottom:0;width:1px` 방식으로 숨김
  - 달력 버튼은 `showPicker()`를 부르고, 안 되는 브라우저는 숨김을 풀고 클릭
- **형 지적 3:** 만 나이의 "기준일" 칸이 뭔지 모르겠다, 하나만 있으면 된다
  → **기준일 입력을 없앰.** 항상 오늘 기준. 관련 코드·설명·meta description 모두 정리.
    (과거·미래 기준 나이는 백 명 중 두세 명이 쓰는 기능인데 칸이 둘이라 모두가 멈칫했음)
- **dday.html 오타 발견:** 날짜 칸 placeholder가 "목표 몸무게 (선택)"였음. 복사하다 남은 것. 제거
- 검증: 1996-09-09 → 만 30세 / 2026-09-08 출생 → 백일 2026.12.16

### v31 (브라우저가 옛 디자인 파일을 기억하던 문제)
- **형 지적:** v30을 올렸는데도 폰 크롬에서 여전히 어둡게 보이고 참새가 이상함
- 원인: **브라우저 캐시.** `assets/style.css`와 `assets/site.js`는 이름이 늘 같아서
  브라우저가 새로 받지 않고 기억해 둔 옛 파일을 씀. HTML만 새로 받으니
  구조는 새것, 색과 움직임은 옛것이 되어 깨져 보였음
- 해결: 54개 페이지에서 불러오는 주소에 **버전 표시**를 붙임
  `assets/style.css?v=30`, `assets/site.js?v=30`, `assets/lotto-data.js?v=30`
- **앞으로 새 버전을 낼 때마다 이 숫자도 같이 올려야 함.** 안 올리면 같은 문제가 반복됨
  (일괄 치환: `?v=이전숫자` → `?v=새숫자`)
- 확인 방법: 시크릿 탭으로 열어 보면 캐시 없이 받아오므로, 시크릿에서만 정상이면 캐시 문제

### v32 (메인 페이지 공유가 안 되던 문제)
- **형 지적:** 계산기 페이지에서는 공유가 되는데 메인에서는 눌러도 아무 일이 없음
- 원인: 공유 코드가 `item.name`을 읽는데, **홈·소개·문의처럼 도구가 아닌 화면에는
  `item`이 없음**(undefined). 거기서 오류가 나며 함수가 그대로 멈췄음
  → `item`이 없으면 `<h1>` 글자를 제목으로 쓰도록 고침
- 확인: index·loan·about 세 곳에서 공유창이 열리는 것과 오류 0건 확인
- **`item`을 쓰는 코드는 항상 없을 때를 함께 생각할 것.** 사이트에는 도구가 아닌
  페이지(홈, 소개, 문의, 개인정보처리방침, 이용약관, 코너 목록)가 여러 개 있음
- 즐겨찾기 버튼이 메인에 없는 것은 그대로 둠. 홈은 첫 화면이라 따로 담을 이유가 없고,
  '자주 쓰는 것' 목록은 도구를 담는 자리임
- 캐시 방지 버전 표시를 `?v=32`로 올림

### v33 (크롬이 강제로 어둡게 만들던 문제)
- **형 지적:** v32까지 올렸는데도 폰 크롬에서 로고가 붉은 검정으로 보임
- 원인: 안드로이드 크롬의 **강제 어둡게(force dark)**. `color-scheme:dark`를 선언해도
  브라우저 설정에 따라 밀어붙이는 경우가 있음. 이 기능은 **배경색과 글자색은 뒤집지만
  그림(SVG)과 사진은 건드리지 않음**
- 해결: 로고 원을 `<span>` + CSS radial-gradient에서 **인라인 SVG `<circle>`**로 바꿈
  (`assets/site.js` 176줄 근처). CSS는 크기·그림자만 남김
- 점검 방법: `--enable-features=WebContentsForceDark --force-dark-mode` 로 크로미움을 띄워
  강제 어둡게 상태를 재현한 뒤 스크린샷 확인. 로고가 노란색으로 유지되는 것 확인함
- **교훈: 브라우저가 색을 뒤집어도 지켜야 하는 것은 CSS 배경이 아니라 SVG로 그릴 것**
- 캐시 방지 버전 표시를 `?v=33`으로 올림

### v33 (브라우저의 강제 어둡게 대응)
- **형 지적:** 삼성 인터넷에서 로고가 검붉게, 글씨가 죽고, 참새가 흐리멍텅함
- **원인은 사이트가 아니라 브라우저 설정이었음.** 삼성 인터넷
  `설정 > 웹페이지 보기 및 스크롤 방식 > 어두운 화면 모드`가 "폰 설정에 따름"이면,
  이미 어두운 사이트를 한 번 더 뒤집어 색이 뭉개짐. "끄기"로 바꾸니 정상
- `color-scheme:dark` 선언으로도 막히지 않음. **사용자가 켠 설정은 사이트가 이길 수 없음**
- 다만 **로고는 CSS 배경 그라데이션에서 인라인 SVG로 교체**함.
  강제 어둡게는 배경·글자색은 뒤집지만 SVG는 건드리지 않으므로 노란 원이 유지됨
  (`--force-dark-mode` 플래그로 크로미움을 띄워 실제로 확인함)
- 앞으로도 **눈에 띄어야 하는 색은 CSS 배경보다 SVG로** 그리는 것이 안전

### v33 (로고를 SVG로)
- **형 지적:** 폰에서 참새방앗간 앞 동그라미가 노란색이 아니라 붉은 검은색
- **진짜 원인은 브라우저 설정이었음.** 삼성 인터넷의
  `설정 > 웹페이지 보기 및 스크롤 방식 > 어두운 화면 모드`가 "폰 설정에 따름"이면,
  이미 어두운 우리 사이트를 **한 번 더 뒤집어** 색이 뭉개짐.
  형이 "끄기"로 바꾸니 정상으로 보였음. 사이트가 막을 수 없는 사용자 설정임
- 다만 그 설정을 켜 둔 방문자도 있으므로 대비함:
  **로고 원을 CSS `radial-gradient` div에서 인라인 SVG로 교체.**
  브라우저의 강제 어둡게는 배경색·글자색은 뒤집지만 **SVG는 건드리지 않음**
- 확인: 크로미움을 `--force-dark-mode`로 띄워 촬영. 그 상태에서도 로고가 노랗게 유지됨
- 캐시 방지 버전 표시를 `?v=33`으로 올림

**앞으로 "어둡게 보인다"는 신고가 오면 브라우저의 어두운 모드 설정부터 확인할 것.**
사이트 코드 문제가 아닐 때가 많음

### v34 (도메인 연결)
- **bangatgan.kr** 구입(가비아, 16,500원) 후 Vercel에 연결
  - 가비아 DNS 관리 > 레코드 수정 > A / @ / 216.198.79.1 / 3600
  - `.com`은 이미 등록되어 있었음. 국내 이용자만 오는 사이트라 `.kr`로 감
  - `.shop`·`.xyz` 같은 저가 확장자는 첫해만 싸고 갱신비가 비싸며, 구글 신뢰도도 낮아 피함
- `sitemap.xml`(54개 주소)과 `robots.txt`를 새 주소로 교체
- **남은 일: 구글 서치 콘솔·네이버 서치어드바이저에 새 주소로 다시 등록.**
  기존 vercel.app 등록은 그대로 두어도 되지만, 새 주소를 주력으로 삼아야 함

### v35 (글 한 편 추가 — 연말정산 시기 전략)
- **info-yearend-plan.html** "연말정산은 12월에 끝납니다" (2026-09-11 작성)
  - 기존 `info-yearend.html`은 *놓치기 쉬운 공제 항목*, 새 글은 *9월~12월에 할 일*. 주제가 겹치지 않음
  - 9~10월 카드 사용액 점검 → 10월 말 홈택스 미리보기 → 11~12월 연금저축 → 12/31 마감 → 1월 간소화
  - 검색으로 확인한 값: 연금저축 단독 600만, IRP 합산 900만,
    총급여 5,500만 이하 16.5%(최대 148.5만원) / 초과 13.2%(118.8만원),
    홈택스 미리보기는 매년 10월 말 개시, 간소화는 1월 15일 무렵
- **새 글을 추가할 때 손댈 곳 네 군데** (하나라도 빠지면 목록·아이콘·의견줄이 안 붙음)
  1. `글파일.html` 새로 만들기 (FAQ 3~4개 + JSON-LD + 작성일 필수)
  2. `assets/site.js`의 **items 배열**에 한 줄 — 이게 있어야 의견 보내기 줄과 아이콘이 붙음
  3. `assets/site.js`의 **아이콘 목록**에 SVG path 한 줄
  4. `info.html`과 해당 분류 페이지(`info-money.html` 등) 목록에 카드 추가 — **최신 글이 맨 위로**
  5. `sitemap.xml`에 주소 추가
- 계절을 타는 주제는 시기를 보고 쓸 것. 9월이면 연말정산 준비, 10월이면 이사철 같은 식

### v36 (네이버 새 주소 소유확인)
- 네이버 서치어드바이저에 `https://bangatgan.kr`를 추가하니 **새 확인 파일**을 요구했음
  (어제 vercel.app용으로 올린 `naver…html`과 파일명이 다름)
- 파일을 또 받아 올리는 대신 **HTML 태그 방식**을 씀. 55개 페이지 `<head>`에 삽입:
  `<meta name="naver-site-verification" content="7ea0de7682fd50f2771c43ebd5a723369427b225" />`
- 구글은 기존 확인 파일이 새 주소에도 그대로 있어 **자동으로 통과**됨
- 구글 사이트맵 제출 완료. 네이버는 소유확인 후 사이트맵 제출 필요
